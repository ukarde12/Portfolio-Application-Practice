$(() => {
    $("*[include-html]").each(function() {
        var htmlFile = $(this).attr("include-html"),
            htmlId = this.id;
        console.log('Value of htmlFile', htmlFile);
        console.log('Value of htmlId', htmlId);
        jQuery.ajax({
            url: htmlFile,
            success: function(file) {
                $("#" + htmlId).html(file)
            },
            error: function(n, c, i) {
                var s = n.
                status + " " + n.statusText;
                $("#" + id).html(file + "-" + s)
            }
        })
    })
})

// This function is used for getting all the include-html tags and in HTML file and take HTML file + attribute then make ajax, We call to read the file

// In case of failure, We will get error code and the actual error